export default class Account {
  constructor(id, user_id, name, amount, remark) {
    this.id = id;
    this.user_id = user_id;
    this.name = name;
    this.amount = amount;
    this.remark = remark;
  }
}